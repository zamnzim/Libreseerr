apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "libreseerr.fullname" . }}
  labels:
    {{- include "libreseerr.labels" . | nindent 4 }}
data:
  TZ: {{ .Values.timezone | default "Etc/UTC" | quote }}
